server:
  port: 8084
spring:
  application:
    name: product-service
  datasource:
    url: jdbc:mysql://sql12.freemysqlhosting.net:3306/sql12709564
    driverClassName: com.mysql.cj.jdbc.Driver
    username: sql12709564
    password: cRBBXNYx2a
    hikari:
      # Consider these connection pool settings for better performance
      poolName: HikariPool
      minimumIdle: 5
      maximumPoolSize: 10
      autoCommit: true
  h2:
    console:
      enabled: true
  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update
    show-sql: true
  cloud:
    function:
      definition: updateCommunication
    stream:
      bindings:
        updateCommunication-in-0:
          destination: communication-sent
          group: ${spring.application.name}
        sendCommunication-out-0:
          destination: sendCommunicationAsProductCreated
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
    connection-timeout: 10s
  management:
    health:
      readiness-state:
        enabled: true
      liveness-state:
        enabled: true
    endpoint:
      health:
        probes:
          enabled: true
    endpoints:
      web:
        exposure:
          include: "*"
  eureka:
    instance:
      preferIpAddress: true
    client:
      fetchRegistry: true
      registerWithEureka: true
      serviceUrl:
        defaultZone: http://localhost:8761/eureka/
